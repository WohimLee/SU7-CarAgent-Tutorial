


```
intent_map.json （完整意图表）
    ↓
embedding 建库（每个 intent 一个向量）
    ↓
用户 query → embedding
    ↓
语义检索 → 找到Top-K candidate intents
    ↓
把 candidate intents 的定义 + 用户 query 注入 Orchestrator Prompt
    ↓
LLM 输出 structured intents（多意图）
    ↓
Task Planner → 子图执行（RAG / 控制 / 导航）

```